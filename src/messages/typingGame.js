// Typing's related messages
const TYPING_GAME = {
    WRONG_CHANNEL: (channelId) => ({content: `Psst...I think you meant to send this in the <#${channelId}> channel.\nBut don't worry, no one noticed!`})
};

export default TYPING_GAME;
